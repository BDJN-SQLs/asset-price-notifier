{
  "name": "asset-price-notifier",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "dependencies": {
    "concurrently": "8.0.1"
  },
  "scripts": {
    "start": "concurrently \"cd ./server && npm start\" \"cd ./clientExpo && expo web\"",
    "postinstall": "cd ./clientExpo && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "typescript": "^5.0.4"
  }
}
